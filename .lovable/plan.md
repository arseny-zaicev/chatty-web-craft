План исправления:

1. **Автоматически помечать Block / Lost как read**
   - Когда сделка перетаскивается в любую стадию со `stage_type = lost` через Pipeline, связанный чат сразу получает `unread_count = 0`.
   - То же самое при смене Stage в карточке сделки и нажатии Save.
   - Если обновление сделки не удалось, откатить optimistic UI как сейчас.

2. **Починить автоматизацию входящих ответов**
   - В `whatsapp-webhook`, когда автоматизация переводит сделку в lost / Block, сразу сбрасывать unread у conversation.
   - Это закрывает кейс, когда человек нажал кнопку/написал “block”, сделка ушла в Block автоматически, а в Inbox продолжает висеть unread.

3. **Сделать unread видимым в Pipeline**
   - Добавить фильтр `Unread` в верхнюю панель Pipeline рядом с assignee filter.
   - Фильтр будет показывать только сделки, у которых связанный conversation имеет `unread_count > 0`, включая сделки в lost / Block стадиях.
   - На карточке сделки показать маленький unread бейдж, чтобы было понятно, где непрочитанные.

Технически трогаю только:
- `src/pages/Pipeline.tsx`
- `src/lib/deals.ts` при необходимости helper-а
- `supabase/functions/whatsapp-webhook/index.ts`

Без изменений схемы базы.