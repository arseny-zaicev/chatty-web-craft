План:

1. Исправить саму нормализацию номеров
- Добавить правило для немецких мобильных: если pipeline содержит `49`, а номер начинается с `15`, `16` или `17` и длина похожа на немецкий mobile, автоматически делать `49 + номер`.
- Примеры:
  - `15774635086` -> `4915774635086`
  - `15209737510` -> `4915209737510`
  - `15735241120` -> `4915735241120`
  - `16095071992` -> `4916095071992`
- Это правило должно срабатывать даже если в pipeline стоят несколько стран `49/43/41`, потому что `15/16/17` - это явный German mobile pattern.

2. Подключить pipeline country codes уже на этапе импорта
- Сейчас `lead-intake` смотрит только `source_connections.config.default_country_code`.
- У Warm Leads source сейчас нет `default_country_code`, поэтому голые номера проходят криво/неоднозначно.
- Нужно передавать в `normalizePhone` список: сначала source default, потом `pipeline.expected_country_codes`.
- Тогда Google Sheets импорт сразу будет чинить номер, а не ждать dispatch/review.

3. Добавить защиту от мусора, но не убивать хорошие номера
- `#ERROR!`, `49`, `49664` и прочий мусор оставлять invalid.
- Номера формата German mobile without country code - чинить.
- Если номер реально неоднозначный и не похож на немецкий mobile - оставлять `needs_review`, не угадывать.

4. Восстановить уже застрявших лидов
- Найти в Warm Leads / DE все `awaiting_manual`/`invalid`/`failed` с bare German mobile pattern.
- Обновить их телефон на нормализованный `49...`.
- Перевести их обратно в очередь отправки (`pending`) только если у них нет уже активного campaign recipient.
- Для тех, кто уже ушёл в failed из-за неправильного номера, создать чистый retry через существующий first-touch flow, а старый failed recipient оставить как историю.

5. Не трогать реальные WhatsApp-undeliverable
- Номера, где уже был Gupshup `#131026 Message Undeliverable` на корректном international номере, не ретраить автоматически.
- Например `491575...`, `43660...`, `90533...` - это уже ответ провайдера, что WhatsApp не найден/недоставляем.

6. Проверить результат
- Протестировать `normalizePhone` на твоих 4 номерах и на мусоре.
- Проверить в базе, что эти 4 стали `49...` и вернулись в очередь.
- Проверить, что новые импорты из Warm Leads сразу нормализуются.